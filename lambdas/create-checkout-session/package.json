{
  "name": "@ldrglprx/create-checkout-session",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/handler.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@my4mlife/stripe-client": "workspace:*",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@types/aws-lambda": "^8.10.149",
    "esbuild": "^0.25.3",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
