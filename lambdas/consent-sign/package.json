{
  "name": "@ldrglprx/consent-sign",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "embed:legal": "node scripts/embed-legal.mjs",
    "prebuild": "pnpm embed:legal",
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/handler.js --external:@aws-sdk/*",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0",
    "@my4mlife/patient-record": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.3",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
